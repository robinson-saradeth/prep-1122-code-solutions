var languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];
console.log('languages array', languages);

var firstElement = 'html';
console.log('the first language of the array is', firstElement);

var thirdElement = 'javascript';
console.log('the 3rd language of the array is', thirdElement);

var length = languages.length;
console.log('the length of the array is', length);

var lastElement = languages[languages.length - 1];
console.log('the last element of the array is', lastElement);
